# Build the C++ control plane for the lazy 1m / 64x1024 CMS variant.
# Sources live in ../cpp_lazy_common/.
BF_SIZE     := 1048576
CMS_BUCKETS := 64
CMS_COLS    := 1024
P4_NAME     := lazy_bf1m
BINARY      := lazy_bf1m_cp
include ../cpp_lazy_common/Makefile.core
