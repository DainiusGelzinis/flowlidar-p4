# Build the C++ control plane for the lazy 131k / 64x1024 CMS variant.
# Sources live in ../cpp_lazy_common/.
BF_SIZE     := 131072
CMS_BUCKETS := 64
CMS_COLS    := 1024
P4_NAME     := lazy_bf
BINARY      := lazy_bf_cp
include ../cpp_lazy_common/Makefile.core
