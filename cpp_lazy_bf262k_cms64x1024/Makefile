# Build the C++ control plane for the lazy 262k variant.
# Sources live in ../cpp_lazy_common/.
BF_SIZE := 262144
P4_NAME := lazy_bf262k
BINARY  := lazy_bf262k_cp
include ../cpp_lazy_common/Makefile.core
