# Build the C++ control plane for the lazy 2m / 64x1024 CMS variant.
# Sources live in ../cpp_lazy_common/.
BF_SIZE     := 2097152
CMS_BUCKETS := 64
CMS_COLS    := 1024
P4_NAME     := lazy_bf2m
BINARY      := lazy_bf2m_cp
include ../cpp_lazy_common/Makefile.core
